/** Default map center: Sri Lanka */
export const DEFAULT_CENTER: [number, number] = [80.7718, 7.8731]; // [lng, lat]

/** Default map zoom level */
export const DEFAULT_ZOOM = 8;

/** Default radius for "near me" queries (km) */
export const DEFAULT_NEAR_RADIUS_KM = 5;

/** Debounce delay for viewport-based fetching (ms) */
export const VIEWPORT_DEBOUNCE_MS = 400;
