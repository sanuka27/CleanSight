# CleanSight ML Phase 2 – Category Classifier Service
# requirements.txt for Hugging Face Spaces Docker deployment

# Web framework
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6

# Pydantic v2 (used in category_service/schemas.py)
pydantic>=2.0.0

# HTTP client for image URL fetching
httpx>=0.24.1

# Image processing
Pillow>=10.0.0

# ML – PyTorch CPU-only (much lighter than CUDA build)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
torchvision>=0.15.0

# Utilities
numpy>=1.26.0
