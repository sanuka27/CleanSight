[pytest]
# Add parent directory to sys.path so 'from ML.service...' works
pythonpath = ..
# Run tests from the ML/tests directory
testpaths = tests
# asyncio mode for pytest-asyncio
asyncio_mode = auto
# Show short test IDs in output
log_cli = true
log_cli_level = WARNING
