# CleanSight ML Phase 2 - PyTorch-based Category Classifier
# Requires Python 3.10+ (tested with Python 3.14.3)
# For Windows installation, see ML/README.md

# PyTorch ecosystem (use --index-url for CPU-only if needed)
# Pinned versions to ensure reproducibility and compatibility
torch==2.5.0
torchvision==0.20.0

# Image processing
Pillow>=10.2.0

# Data science utilities
numpy>=1.26.0
matplotlib>=3.8.0
seaborn>=0.13.0
scikit-learn>=1.4.0
